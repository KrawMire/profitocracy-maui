import { StyleSheet } from "react-native";

export const sharedTextStyle = StyleSheet.create({
  screenTitle: {
    fontSize: 30,
    fontWeight: "700",

    // TODO: Temporary
    marginBottom: 15
  },
  sectionTitle: {
    fontSize: 20,
    fontWeight: "600",

    // TODO: Temporary
    marginBottom: 10
  }
});