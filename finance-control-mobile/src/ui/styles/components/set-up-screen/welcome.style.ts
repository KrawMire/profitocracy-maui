import { StyleSheet } from "react-native";

export const welcomeStepStyles = StyleSheet.create({
  moveNextButton: {
    marginLeft: "10%",
    marginTop: 20,
    width: "80%",
  },
  welcomeText: {
    marginTop: 20,
    textAlign: "center",
  },
});
